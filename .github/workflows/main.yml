name: Deploy FaithPod Frontend to Contabo

on:
  push:
    branches:
      - main

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout Code
        uses: actions/checkout@v3

      - name: Print GITHUB_WORKSPACE
        run: echo "Workspace:$GITHUB_WORKSPACE"

      - name: Set up Node.js
        uses: actions/setup-node@v3
        with:
          node-version: 20

      - name: Clean and Install Dependencies
        run: |
          rm -rf node_modules package-lock.json
          npm cache clean --force
          npm install --legacy-peer-deps

      - name: Build Angular App
        run: npm run build -- --configuration=production

      - name: Debug Check Build Directory
        run: ls -R dist/

      - name: Deploy Build Files to Server
        uses: appleboy/scp-action@v0.1.4
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2150
          source: "dist/faithpod-frontend/browser/*"
          target: "/home/${{ secrets.VPS_USER }}/faithpod-frontend-temp"
          strip_components: 3

      - name: Deploy to Contabo Server via SSH
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2150
          script: |
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S mkdir -p /var/www/html/faithpod-frontend
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S rsync -avz --delete /home/${{ secrets.VPS_USER }}/faithpod-frontend-temp/ /var/www/html/faithpod-frontend/
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S chown -R www-data:www-data /var/www/html/faithpod-frontend
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S chmod -R 755 /var/www/html/faithpod-frontend
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S a2enmod headers rewrite || true
            rm -rf /home/${{ secrets.VPS_USER }}/faithpod-frontend-temp

      - name: Restart Apache
        uses: appleboy/ssh-action@v0.1.6
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USER }}
          key: ${{ secrets.SSH_PRIVATE_KEY }}
          port: 2150
          script: |
            echo "${{ secrets.SUDO_PASSWORD }}" | sudo -S systemctl restart apache2
