export const environment = {
  production: true,
  apiUrl: "https://api.faithpod.io/api", // Central admin API (admin.faithpod.io)
  apiPort: "", // No port in production (uses default 80/443)
  defaultTenant: null // No default tenant in production
};
